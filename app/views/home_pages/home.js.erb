$('.post-wrapper').append('<%= j render(@posts) %>');

<% if @posts.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate(@posts) %>');
<% else %>
  $('.pagination').remove();
  //$('#loadmore').remove(); //load more comment or post when button is applied
<% end %>